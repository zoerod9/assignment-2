// sorts the sub-array given from main using insertion sort or selection sort 
// (one is enough and you need to implement either one), 

// and computes the average of the values in the given array. 

// So this thread returns sorted sub-array and its average.
#include  <stdio.h>
int main (){
    puts("hello from sorting_avg");
    return 0;
}