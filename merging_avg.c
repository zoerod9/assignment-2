// takes both sorted sub-arrays and the their averages as parameters. 

// It then merges them into a single sorted array in a linear time 
// (so don't call sorting function again! You can simply have a loop and take 
// the smallest from the sorted arrays until they are all merged!), 

// and simply find the overall average by taking the average of the given 
// two averages (this will work because we assume both sub-arrays have the same
//  size!). 
#include  <stdio.h>
int main (){
    puts("hello from merging_avg");
    return 0;
}